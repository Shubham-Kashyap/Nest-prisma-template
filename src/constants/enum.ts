export enum IncommingRequestLogging {
    enabled = '1',
    disabled = '0'
}

export enum Role {
    User = 'user',
    Admin = 'admin',
    Any = 'any'
}

export enum PageSize {
    Min = 10,
    Max = 100
}
